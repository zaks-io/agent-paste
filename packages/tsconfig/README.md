# tsconfig

Shared TypeScript configuration ownership package.

The root [`tsconfig.base.json`](../../tsconfig.base.json) is currently canonical. Keep this package as the workspace owner for future package-published presets if the monorepo needs them.
