# tsconfig

Planned shared TypeScript configuration package.

The root [`tsconfig.base.json`](../../tsconfig.base.json) is present now. Move or wrap it here if the implementation needs package-published tsconfig presets.
