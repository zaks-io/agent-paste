# actionlint config. Registers the Blacksmith custom runner labels so actionlint
# does not flag them as unknown. See https://github.com/rhysd/actionlint/blob/main/docs/config.md
self-hosted-runner:
  labels:
    - blacksmith-2vcpu-ubuntu-2404
    - blacksmith-2vcpu-ubuntu-2404-arm
    - blacksmith-6vcpu-macos-latest
    - blacksmith-4vcpu-windows-2025
