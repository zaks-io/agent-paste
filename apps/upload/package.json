{
  "name": "@agent-paste/upload",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "check": "pnpm typecheck",
    "deploy:live": "pnpm deploy:production",
    "deploy:preview": "wrangler deploy --config wrangler.jsonc --env preview",
    "deploy:production": "wrangler deploy --config wrangler.jsonc --env production",
    "dev": "wrangler dev --config wrangler.jsonc",
    "lint": "biome lint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage --config vitest.config.ts",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "typegen": "wrangler types src/worker-configuration.d.ts --config wrangler.jsonc --env preview"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@agent-paste/auth": "workspace:*",
    "@agent-paste/commands": "workspace:*",
    "@agent-paste/contracts": "workspace:*",
    "@agent-paste/db": "workspace:*",
    "@agent-paste/rotation": "workspace:*",
    "@agent-paste/storage": "workspace:*",
    "@agent-paste/tokens": "workspace:*",
    "@agent-paste/worker-runtime": "workspace:*",
    "@sentry/cloudflare": "^10.57.0",
    "hono": "^4.12.21",
    "jose": "^6.2.3"
  }
}
